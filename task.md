# Lista de Tareas: GeoRiesgos Saltillo

Esta es la lista de tareas activas para el desarrollo del proyecto. Usa `[ ]` para tareas pendientes, `[/]` para tareas en progreso y `[x]` para completadas.

---

## Fase 1: Entorno de Desarrollo y Configuración Inicial
- [x] Configurar repositorio Git e inicializar commits (`git init`, `.gitignore`).
- [x] Crear el entorno virtual de Python (`venv`) e instalar dependencias (`pandas`, `geopandas`, `jupyter`, `shapely`, `pyproj`). Nota: se omitió `fiona` (sin wheel para Python 3.14 en Windows, requiere GDAL); GeoPandas 1.x usa `pyogrio` como motor de I/O por defecto, cubriendo la misma función.
- [/] Descargar los primeros conjuntos de datos brutos:
  - [/] Polígonos de AGEB de Coahuila (INEGI, *Información vectorial de localidades amanzanadas y números exteriores 2023* — ver `DATOS.md` §2.1; no es el Marco Geoestadístico). Ya en `raw_data/marco_geoestadistico/saltillo_map_ageb/` (la carpeta conserva el nombre viejo) — cubre únicamente municipio de Saltillo (localidad Saltillo ciudad con 342 AGEBs + 3 localidades rurales sin AGEB). Falta Ramos Arizpe y Arteaga.
  - [/] Censo de Población 2020 a nivel de AGEB/Manzana para Saltillo (INEGI). Descargado por el usuario, pendiente indicar la ruta local para integrarlo.
  - [x] Capas de riesgos de inundación y naturales en Saltillo. **Fuente primaria: IMPLAN Saltillo — CARTO SALTILLO, Atlas de Riesgos 2024** (SHP vectoriales, EPSG:6372, sin login). Descargados por el usuario a `raw_data/`: `Riesgo_por_inundaciones_pluviales3`, `SUSCEPTIBILIDAD_INUNDACIONES_PLUVIALES` y `Riesgo_por_Deslizamientos_traslacionales2`. **Respaldo secundario: ANRI de CONAGUA** (raster de severidad Tr=100 vía servicio ArcGIS REST) — se conserva en `data/riesgo_inundacion.png` + `_meta.json` y en `descargar_raster_inundacion()`. IMPLAN es primaria por ser local, vectorial y de 2024.
  - [x] Registrar estrictamente la fecha de descarga y la fuente oficial de cada dataset en una bitácora del proyecto para asegurar la legitimidad del análisis. Bitácora en `DATOS.md`: ficha por dataset (fuente oficial, editor, fecha de corte, fecha de descarga, ruta local, licencia y uso), datasets descartados con su motivo, trazabilidad de cada capa publicada y pendientes. Las fechas se verificaron leyendo los metadatos internos de cada paquete (`metadatos/*.txt` de INEGI, `*.shp.xml` de los SHP), no de memoria.

## Fase 2: Procesamiento de Datos (Python & GeoPandas)
- [x] Filtrar la capa de AGEB para obtener únicamente Saltillo, Ramos Arizpe y Arteaga. Implementado en `scripts/process_data.py` (`filtrar_agebs_por_municipio`). Por ahora solo Saltillo tiene datos (342 AGEBs); la config `MUNICIPIOS_AGEB` ya tiene entradas listas para Ramos Arizpe y Arteaga — solo falta agregar las rutas de sus carpetas de localidad cuando se descarguen. Salida intermedia en `raw_data/processed/ageb_filtrado.geojson` (EPSG:4326).
- [x] Procesar los datos de servicios del Censo e integrarlos por colonia/AGEB. Implementado en `scripts/process_data.py` (`cargar_censo_servicios`, `calcular_cobertura_servicios`, `integrar_censo_a_ageb`). Calcula % de electricidad, agua, drenaje e internet por AGEB (variables `VPH_C_ELEC`, `VPH_AGUADV`, `VPH_DRENAJ`, `VPH_INTER` sobre `TVIVHAB`) y un índice compuesto `SERVICIOS_INDEX` (promedio de los 4). Nota: el Censo agregado por AGEB no permite calcular "% viviendas con TODOS los servicios simultáneamente" (eso requeriría microdatos) — `SERVICIOS_INDEX` es la aproximación por promedio. 340/342 AGEBs de Saltillo con datos (2 sin match, probablemente no residenciales). Salida intermedia en `raw_data/processed/ageb_con_servicios.geojson`.
- [x] Cruzar espacialmente los AGEB con las zonas de inundación (IMPLAN). Implementado en `scripts/process_data.py` (`calcular_riesgo_inundacion_por_ageb`): overlay vectorial AGEB↔riesgo de inundación en CRS métrico (EPSG:6372), calculando `RIESGO_INDEX` 0-100 por AGEB como exposición ponderada por área × intensidad del nivel. 204/340 AGEBs con exposición >0 (máx 70.6). Trivial gracias a que IMPLAN es vectorial (no requirió leer rasters ni dependencias nuevas).
- [x] Exportar las capas resultantes a formato GeoJSON limpio en la carpeta `data/`. Implementado en `scripts/process_data.py` (`exportar_capa_servicios_basicos`): geometría simplificada (tolerancia 0.00005°) y solo columnas relevantes para el frontend. Resultado: `data/servicios_basicos.geojson`, 340 AGEBs, 477.8 KB (muy por debajo del límite de 5MB de `SPEC.md`), sin geometrías inválidas ni vacías. Falta aún la capa de inundación (bloqueada) y el Índice de Inversión (Fase 5).

## Fase 3: Mapa Base Interactivo (Leaflet.js)
- [x] Crear la estructura de la página en `index.html` importando Leaflet.js. Ya existía del scaffold inicial.
- [x] Aplicar estilos premium en `styles.css` (tema oscuro, tipografía Inter, paneles flotantes glassmorphic). Ya existía del scaffold inicial.
- [x] Programar en JavaScript la inicialización del mapa centrado en Saltillo y Ramos Arizpe. Cambiado de centro/zoom fijo a `map.fitBounds()` con una región que cubre Saltillo, Ramos Arizpe y Arteaga. Verificado en navegador (zoom 11, bounds correctos tras reload).
- [x] Implementar el control de capas base y el panel lateral interactivo. Agregado `L.control.layers` (Oscuro/Claro) abajo a la izquierda, y wiring del botón de cierre del sidebar de detalles (`#close-sidebar`). Ambos verificados funcionando en el preview del navegador. Nota: los checkboxes de capas de datos (`#layer-services`, etc.) son solo UI por ahora — su lógica real de mostrar/ocultar capas GeoJSON es Fase 4.

## Fase 4: Integración de Capas en el Mapa
- [x] Cargar los archivos GeoJSON locales usando Fetch API en JavaScript. `fetch('data/servicios_basicos.geojson')` en `index.html`. Verificado en navegador: 340 features cargadas.
- [x] Estilizar la capa de Servicios Básicos según el porcentaje de cobertura (de menor a mayor cobertura). Choropleth de 5 escalones (rojo→verde) por `SERVICIOS_INDEX`, con resaltado al pasar el mouse.
- [x] Estilizar las capas de Riesgos Naturales (choropleth semafórico semitransparente por nivel de intensidad). Dos capas vectoriales IMPLAN: **Riesgo de Inundación** (Capa 1, `data/riesgo_inundacion.geojson`, 648 KB) y **Riesgo de Deslizamientos** (Capa 4 geológica nueva del SPEC, `data/riesgo_deslizamientos.geojson`, 107 KB). Se descarta el nivel "Muy bajo" (fondo del ~90-98% del área, infla el archivo sin valor de riesgo) y se disuelve por nivel. Cargador `cargarCapaRiesgo` en `index.html` (nuevo checkbox `#layer-landslides` para la Capa 4), click→ficha con intensidad + fuente/fecha de corte (SPEC §1.2), y `preferCanvas: true` para rendimiento con multipolígonos densos (disueltos de ~12,700). Verificado en navegador: render, leyenda, click y toggles independientes correctos, sin errores de consola.
- [x] Crear leyendas interactivas y tooltips informativos para el usuario al hacer clic en una zona. Leyenda dinámica por capa activa + sidebar con ficha de detalle al hacer clic (población, viviendas, % de cada servicio, índice compuesto). Checkboxes de capas sin datos (`layer-floods`, `layer-investment`) deshabilitados visualmente hasta que existan. Todo verificado interactivamente en el preview del navegador (toggle on/off, click en polígono, leyenda).
- [x] Extra: nombre de colonia por AGEB. Los AGEB no traen nombre propio (son unidades estadísticas); se derivó de la capa "Frente de manzana" (`fm`, `NOMASEN`) del paquete completo de INEGI (`raw_data/marco_geoestadistico/saltillo_map_ageb/050300001/conjunto_de_datos/050300001fm.dbf`, antes faltante) tomando el nombre de asentamiento más frecuente por AGEB. Nueva función `cargar_nombres_colonias()` en `scripts/process_data.py`. El sidebar ahora usa el nombre de colonia como título en vez de "Detalles del Sector". Verificado en navegador con prueba atómica (clic disparado y título comparados en la misma llamada).

## Fase 5: Algoritmo de Índice y Despliegue
- [x] Programar la lógica del Scoring de inversión en Python y generar la capa consolidada. Implementado en `scripts/process_data.py` (`cargar_denue`, `calcular_indice_comercios`, `calcular_indice_inversion`, `exportar_capa_indice_inversion`). El componente "Comercios" se calcula con el DENUE (escuelas=SCIAN 61, salud=SCIAN 62, supermercados) como cercanía del centroide de cada AGEB al establecimiento más próximo de cada categoría (decaimiento lineal hasta 3 km). **Riesgo reincorporado:** el componente "Riesgo" (peso 0.3) ya se aplica como penalización (hasta −30 pts) usando la exposición a inundación IMPLAN por AGEB (`RIESGO_INDEX`, ver Fase 2). El índice base es Servicios (0.4) + Comercios (0.3) renormalizado a 0-100, menos la penalización, recortado a [0,100]. Verificado (ej.: EL DIVISADERO base 65.5 − 21.2 = 44.3). Salida: `data/indice_inversion.geojson` (340 AGEBs, 506 KB).
- [x] Agregar la capa del "Índice de Inversión" al mapa de Leaflet con una escala de color verde-rojo. Checkbox habilitado, choropleth de 5 escalones (mismos cortes que Servicios), leyenda y ficha de detalle propia (`mostrarDetalleInversion`: desglose de escuelas/salud/supermercados + índice de comercios + índice final). Se generalizó el sistema de leyendas para soportar varias capas activas a la vez (muestra la de activación más reciente). Verificado en navegador con pruebas atómicas (clic, alternado de capas).
- [x] Realizar pruebas locales finales levantando el servidor ligero de Python. Verificado con `python -m http.server`: sin errores de consola ni de red, ambas capas cargan y se pueden alternar, sidebar y leyenda funcionan, escritorio y móvil (375×812) probados. **Bug encontrado y corregido:** en el media query móvil, la regla base `#details-sidebar` traía `transform: translateY(450px)` fijo (no solo en `.sidebar-collapsed`); como un selector de ID tiene más especificidad que uno de clase, esto dejaba el sidebar de detalles permanentemente fuera de pantalla en móvil sin importar su estado. Bug preexistente del scaffold inicial, no introducido en esta sesión. Corregido en `styles.css` quitando esa línea redundante; verificado con ciclo completo cerrado→abierto→cerrado en viewport móvil.
- [x] Configurar GitHub Pages para desplegar el mapa en la web de manera gratuita. **(Completado por el usuario)**
- [x] Investigar desastres naturales potenciales específicos de la región (deslaves de laderas, fallas geológicas locales) mediante noticias o el Atlas de Riesgos de Coahuila, evaluando si representan un impacto real que justifique incluirlos. **Encontrados en CARTO SALTILLO (IMPLAN).**
- [ ] Asegurar una arquitectura modular de datos para facilitar la expansión a Monterrey, Torreón o Monclova.

## Backlog / Próxima sesión
- [x] **Nombre de colonia en las capas de riesgo.** Al hacer clic en una zona de inundación o deslizamiento, la ficha ahora titula con la colonia que contiene el punto y muestra su municipio, además de fenómeno, intensidad y fuente/fecha. Implementado en `index.html` con un point-in-polygon propio (ray-casting, ~40 líneas, soporta Polygon/MultiPolygon y huecos) contra los AGEB de `servicios_basicos.geojson`, que se indexan al cargar (con bbox precalculada) independientemente de si esa capa está visible. Se descartó precomputarlo en Python: las capas de riesgo se disuelven por nivel, así que una figura abarca decenas de colonias y habría que dejar de disolver, disparando el tamaño del archivo. Se descartó Turf.js por no sumar una dependencia para un cálculo puntual (~340 polígonos, un clic aislado). **Verificación:** (1) cruce contra GeoPandas/shapely `within` sobre una rejilla de 2,025 puntos (1,124 dentro / 901 fuera) — coincidencia exacta en los 2,025, mismo hash de la secuencia completa de respuestas; (2) clics reales en el navegador sobre inundación (RANCHO LAS VARAS, Muy alto) y deslizamientos (PUERTO DE FLORES, Bajo); (3) en el mismo píxel, la capa de servicios (donde la colonia la resuelve el hit-testing de Leaflet, no mi código) devuelve la misma colonia; (4) sin errores de consola y la ficha de servicios sin regresiones. **Hallazgo:** las dos capas de riesgo están íntegramente contenidas en la cobertura de AGEBs (1095/1095 y 195/195 puntos representativos), así que el caso "fuera de zona urbana" no ocurre con los datos actuales; el respaldo se conserva por si se suman municipios. Sí se distingue el caso real de que el clic llegue antes de que carguen los AGEB ("Cargando…"), que de otro modo habría dicho "Fuera de zona urbana" — una mentira. Documentado en `DATOS.md` §4 que la ficha de riesgo combina dos fuentes (intensidad del IMPLAN, colonia de INEGI).
- [x] **Bitácora de datos.** Completada en `DATOS.md` (ver Fase 1). Hallazgos al verificar los metadatos oficiales: (1) lo que citábamos como "Marco Geoestadístico" es en realidad *Información vectorial de localidades amanzanadas y números exteriores 2023* de INEGI — un producto distinto que solo usa el Marco (dic. 2022) como capa base; (2) el DENUE es la edición 05_2026, corte 2026-05-20; (3) las capas de riesgo del IMPLAN acreditan al Instituto de Geografía de la UNAM en el caso de deslizamientos, y sus archivos declaran fechas de 2025 aunque el portal etiqueta el producto como "Atlas de Riesgos 2024" (se interpretan como fechas de exportación; `IMPLAN_FECHA_CORTE = "2024"` se mantiene, es como lo nombra el editor).
- [x] **Corregir la atribución del producto de AGEB** donde se le llamaba "Marco Geoestadístico"; el nombre correcto es *INEGI — Información vectorial de localidades amanzanadas y números exteriores 2023* (ver `DATOS.md` §2.1). Corregido en `README.md`, `CLAUDE.md`, `SPEC.md` (que además no citaba la fuente de los AGEB en absoluto; ahora la declara en §2 "Unidad Territorial Base"), `scripts/process_data.py` y las notas de este archivo. En el script se renombró `filtrar_marco_geoestadistico` → `filtrar_agebs_por_municipio` (el nombre se leía desde el `main` y era el que más engañaba); verificado corriendo el pipeline completo: los 4 GeoJSON salen byte-idénticos. La carpeta `raw_data/marco_geoestadistico/` conserva su nombre a propósito: renombrarla rompería las copias locales de datos que no están versionados, a cambio de nada. Nota: la confusión venía de que INEGI clasifica el producto bajo el *tema* "Marco Geoestadístico" y lo publica en el mismo portal, pero es un producto distinto que solo lo usa como capa base.
- [x] **README actualizado.** Afirmaba que la capa de inundación estaba pendiente "por falta de datos disponibles" (lleva sesiones implementada con IMPLAN) y su sección "Fuentes de datos" omitía por completo al IMPLAN y a CONAGUA — incumplimiento de SPEC §1.2 en la cara pública del proyecto. Ahora lista las 4 capas reales, las 5 fuentes con enlace, apunta a `DATOS.md`, refleja el estado real (falta AGEB de Ramos Arizpe/Arteaga y fuente de riesgo forestal) y advierte que los datos de riesgo son modelos a escala urbana, no un estudio de sitio.
- [ ] **Índice de incendio / riesgo forestal (SPEC Capa 2).** Investigar fuentes (CONABIO/CONAFOR, IMPLAN). **Primero verificar si es realmente útil y si existen datos granulares para la zona (Arteaga / Sierra) antes de implementar**; si no aporta valor real, descartarlo explícitamente con justificación.
- [ ] **Identidad del proyecto.** Explorar un nombre (candidato inicial: "La_Map") y una identidad visual/tema coherente (logo, paleta, tipografía) que haga match con la aplicación.
